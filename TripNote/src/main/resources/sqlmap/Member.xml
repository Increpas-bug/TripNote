<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bug.tripnote.dao.MemberMapper">

	<resultMap type="Member" id="MemberResultSet">
		<id property="user_no" column="user_no" />
		<result property="user_email" column="user_email" />
		<result property="user_nickname" column="user_nickname" />
		<result property="user_pw" column="user_pw" />
		<result property="user_birth" column="user_birth" />
		<result property="user_adminyn" column="user_adminyn" />
	</resultMap>
	
	
	
	<!-- 회원가입 멤버 추가 -->
	<insert id="insertMember" parameterType="Member">
	insert users (user_no,user_email,user_nickname,
	user_pw,user_birth,user_adminyn)
	 
	VALUES(user_seq.nextvalus,#{user_no},#{user_email},#{user_nickname},
	#{user_pw},#{user_birth},#{user_adminyn})
	</insert>
	
	<!-- VALUES(user_seq.nextvalus,#{user_email},#{user_nickname},
	#{user_pw},#{user_birth},#{user_adminyn})
	 -->
	
	<!-- 회원 탈퇴 -->
	
	<delete id="deleteMember">
	delete users 
	
	</delete>
	
	
	
<!-- 
	<resultMap type="#modelname" id="YourResultSet">
		<id property="param1" column="columnname1" />
		<result property="param2" column="columnname2" />
		<result property="param3" column="columnname3" />
	</resultMap>

	<select id="getAll" resultMap="YourResultSet">
		SELECT * FROM tablename1
	</select>

	<insert id="insertSomething" parameterType="#modelname">
		INSERT INTO tablename1 (columnname1, columnname2, columnname3)
		VALUES(#{value1},#{value2},#{value3})
	</insert>

	<update id="updateSomething" parameterType="#modelname">
		UPDATE tablename1
		SET
		columnname1=#{param1}
		WHERE
		columnname2 = #{param2}
	</update>

	<delete id="deleteSomething" parameterType="int">
		DELETE FROM tablename1 WHERE
		columnname1 = #{param1}
	</delete>
 -->
</mapper>
